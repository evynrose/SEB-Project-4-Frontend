export interface ICondition {
    users: string, 
    _id: string, 
    name: string, 
    about: string, 
    info: string, 
    user: string, 
    advice: string, 
    
}

type isActive = boolean

//interfaces to fix typescript errors